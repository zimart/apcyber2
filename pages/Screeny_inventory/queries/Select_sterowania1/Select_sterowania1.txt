SELECT
  *
FROM
  sterowania
WHERE
  model LIKE '%{{Table1.searchText}}%'
ORDER BY
  {{Table1.sortOrder.column || 'id_sterowania'}} {{Table1.sortOrder.order !== "desc" ? "" : "DESC"}}
LIMIT
  {{Table1.pageSize}}
OFFSET
  {{Table1.pageOffset}}