UPDATE screeny_inventory SET
systemzdoplata = CASE 
   {{tabela_screeny_inventory_id.updatedRows.map((user) => `WHEN id = ${user.id} THEN '${user.updatedFields.systemzdoplata}'`).join('\n')}}
END

WHERE id IN ({{data_table.updatedRows.map((user) => user.allFields.id).join(',')}});